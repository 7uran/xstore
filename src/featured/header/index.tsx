import React from 'react';
import Image from 'next/image';

const Header: React.FC = () => {
    return (
        <header className="w-full bg-red-900 p-4 flex justify-between items-center">

        </header>
    );
};

export default Header;
